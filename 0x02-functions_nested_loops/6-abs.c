#include "main.h"

/**
 * _abs - a function that compute the absolute value of a given integer
 * @c: single number input
 * Return: the absolute number
 */
int _abs(int c)
{
return (c * ((c > 0) - (c < 0)));
}
